// add solution here
const array = [];

function theBeatlesPlay (musicians, instruments) {
  for (let i=0; i < 4; i++) {
    if (i === 0) {
      return `${musicians[0]} plays ${instruments[0]`
    }
  }
}